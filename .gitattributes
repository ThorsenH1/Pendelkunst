# Normalize line endings: store text as LF in the repo, check out native locally.
* text=auto eol=lf

# Treat binary assets as binary (no EOL normalization / diffing).
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
